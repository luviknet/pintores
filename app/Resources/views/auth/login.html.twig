{% extends 'base.html.twig' %}
{% block body %}
<body class="login-page">
 <div class="content card" style="width: 50%; margin: auto;">
 
      <div class="row" style="padding:10px;">
        
      <div class="col-md-6">
              <h5 class="titulo-login">
        LOGIN CON CUENTA
   </h5>  
                            <form method="POST" action="{{ path('security_login_check') }}">
                {% if error %}
                <div class="alert alert-danger">
                  {{ error.messageKey|trans(error.messageData) }}
                </div>
                {% endif %}

              
                <div class="form-group">
                  <div class="form-line">
                    <input type="text" id="email" name="_email" value="{{ last_username }}" class="form-control" placeholder="Ingresar Email">
                  </div>
                </div>
                
                <div class="form-group">
                  <div class="form-line">
                    <input type="password" id="password" name="_password" class="form-control" placeholder="Ingresar Contraseña">
                  </div>
                </div>

                <input type="checkbox" id="remember_me" class="filled-in">
                <label for="remember_me">Recordarme</label>
                <br>
                <button type="submit" class="btn bg-red btn-block btn-lg  m-t-15 waves-effect">INGRESAR</button>
              </form>
 
        </div>
 <div class="col-md-6">
   
   
     <h5 class="titulo-login">
        LOGIN CON FACEBOOK
   </h5>  
        
        <p class="texto-login">
          *Todos los datos proporcionados por tu perfil de Facebook son confidenciales. Pintores no hará uso de los mismos ni publicará información en tu muro.
        </p>
    <button type="submit" class="btn bg-cyan btn-block btn-lg m-t-30 waves-effect">INGRESAR CON FACEBOOK</button>
   
  
          </div>
      </div>
      </div>
{% endblock %}